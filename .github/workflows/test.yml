name: Release

on:
  pull_request

jobs:
   check-version:
    runs-on: macOS-11
    if: contains(github.base_ref, 'master'
    steps:
      - uses: actions/checkout@master
      - name: base_ref master
        run: echo 'test_branch'
      - name: base_ref master 2
        run: echo 'test_branch 2'
        
   job2:
    runs-on: macOS-11
    if: contains(github.head_ref, 'test_branch')
    steps:
      - name: head_ref release
        run: echo 'Master'
      - name: head_ref release 2
        run: echo 'Master 2'
#         run: |
#           echo ::set-output name=VERSION::${GITHUB_REF/refs\/tags\//}
#       - name: Update pod file
#         run: |
#           sed -i '' 's/0.0.7/${{ steps.get_version.outputs.VERSION }}/' ./TestCocoaPods_test_private.podspec
#       - name: Commit
#         run: |
#           # Stage the file, commit and push

#           git checkout master
#           git add .
#           git commit -m "Added framework"
#           git push origin master --force

   
